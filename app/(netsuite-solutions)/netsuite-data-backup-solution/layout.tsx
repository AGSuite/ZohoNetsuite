import { metadata } from "./metadata";

export { metadata };

export default function NetSuiteDataBackupSolutionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
